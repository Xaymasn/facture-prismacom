<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="facture-prismacom.list">
      <field name="name">facture-prismacom list</field>
      <field name="model">facture-prismacom.facture-prismacom</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="facture-prismacom.action_window">
      <field name="name">facture-prismacom window</field>
      <field name="res_model">facture-prismacom.facture-prismacom</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="facture-prismacom.action_server">
      <field name="name">facture-prismacom server</field>
      <field name="model_id" ref="model_facture-prismacom_facture-prismacom"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="facture-prismacom" id="facture-prismacom.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="facture-prismacom.menu_1" parent="facture-prismacom.menu_root"/>
    <menuitem name="Menu 2" id="facture-prismacom.menu_2" parent="facture-prismacom.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="facture-prismacom.menu_1_list" parent="facture-prismacom.menu_1"
              action="facture-prismacom.action_window"/>
    <menuitem name="Server to list" id="facture-prismacom" parent="facture-prismacom.menu_2"
              action="facture-prismacom.action_server"/>
    -->
  </data>
</odoo>